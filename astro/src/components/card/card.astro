---
import { cn } from '../../../common/utils';

export type CardProps = {
    className?: string;
}

const { className, ...props} = Astro.props as CardProps;

---
<div class={cn('rounded-lg border bg-card text-card-foreground shadow', className)} {...props}>
    <slot />
</div>