---
import { cn } from '../../../common/utils';
import { Props } from './card.astro';

const { className, ...props} = Astro.props as Props;

---
<div class={cn('flex flex-col space-y-1.5 p-6', className)} {...props}>
    <slot />
</div>